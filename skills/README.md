# Skills

This directory is optional.

Use it only when a project needs local skill wrappers or project-specific adapters.

For most external skills, declaring them in `standards/skills/manifest.yaml` is enough.
